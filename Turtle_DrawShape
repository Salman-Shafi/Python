import turtle 

shape = turtle.Turtle()

num_sides = 6
side_length = 50
angle = 360.0 / num_sides 

for i in range(num_sides):
    shape.forward(side_length)
    shape.right(angle)
    
turtle.done()
